name: Setup Utoo
description: Download, install, and setup Utoo to your path.
author: utooland

branding:
  icon: package
  color: purple

inputs:
  utoo-version:
    description: The version of Utoo to install. (e.g. "latest", "1.0.0", "1.0.x")
    required: false
    default: latest
  registry:
    description: The URL of the npm registry to use for installing Utoo.
    required: false
    default: https://registry.npmjs.org/
  cache-store:
    required: false
    type: boolean
    default: false
    description: Cache npm store directory (~/.cache/nm) for faster package installations.
  cache-utoo:
    required: false
    type: boolean
    default: true
    description: Cache installed Utoo binary to avoid reinstallation on subsequent runs.
  utoo-tgz:
    description: URL or path to a pre-built utoo .tgz package. When set, installs from this tgz instead of the npm registry.
    required: false
    default: ''

outputs:
  utoo-version:
    description: The version of Utoo that was installed.

runs:
  using: "node20"
  main: "dist/setup/index.js"
  post: "dist/cache-save/index.js"
  post-if: success()
